import 'package:flutter/material.dart';

class RawTextfield extends StatelessWidget {
  const RawTextfield({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
